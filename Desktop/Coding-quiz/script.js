//global variables that 
var score = 0;
var timeleft = 200;
var questions = [];
var answerChoices = [["...", "...", "... ", "...",]]; //made an array of strings, need to fill with dofferent choices for the quiz
var correctAnswer = []; //made of strins

